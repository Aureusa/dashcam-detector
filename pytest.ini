[pytest]
testpaths = tests
addopts = -ra
markers =
    slow: long-running test (starts the full app / real model)
    gpu: needs the real detector (torch + model weights); skipped if unavailable
filterwarnings =
    ignore::DeprecationWarning:werkzeug.*

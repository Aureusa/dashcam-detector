# Dashcam RT-DETR pipeline — pinned versions (Python 3.12, Ubuntu 24.04).
#
# PyTorch must be a CUDA build that supports the GPU. The RTX 50-series (Blackwell, sm_120)
# needs CUDA >= 12.8; this environment uses the CUDA 13.0 wheels (driver >= 580):
#   pip install torch==2.14.0 torchvision==0.29.0 --index-url https://download.pytorch.org/whl/cu130
# (cu128 alternative: --index-url https://download.pytorch.org/whl/cu128, torch<=2.11)
# then:
#   pip install -r requirements.txt
--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.14.0+cu130
torchvision==0.29.0+cu130

transformers==5.17.0
huggingface_hub==1.32.0
safetensors==0.8.0
# Only the headless OpenCV wheel — never install opencv-python alongside it.
opencv-python-headless==5.0.0.93
flask==3.1.3
numpy==2.5.2
pyyaml==6.0.3
pillow==12.3.0
pytest==9.1.1
